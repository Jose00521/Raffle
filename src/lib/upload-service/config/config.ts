const uploadcConfig = {
        MAX_FILE_SIZE : 5 * 1024 * 1024,
        ALLOWED_MIME_TYPES : ['image/jpeg', 'image/png', 'image/webp', 'image/heic'],
        IMAGE_QUALITY : 80,
        MAX_WIDTH : 1200,
        MAX_HEIGHT : 1200,
}

export default uploadcConfig;